# Uncomment the next line to define a global platform for your project
# platform :ios, '13.0'

target 'SwiftUI-MVVM-Sample (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftUI-MVVM-Sample (iOS)
pod 'Alamofire'
pod 'SwiftLint'

end

target 'SwiftUI-MVVM-Sample (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftUI-MVVM-Sample (macOS)

end
